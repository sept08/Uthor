import * as React from 'react';
import ReactDOM from "react-dom";

const Render = () => {
  return (
    <div>render</div>
  );
};

ReactDOM.render(<Render />, document.getElementById('ice-container'));
